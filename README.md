# permissions-system-sim
A payground for simulationg flows in the consento permissions system

## TODO Tests:

- one member does an operation while another removes that member: who wins?
- Multiple requests processed at once where the first adds a member (preventing the second to go through because more request are needed)
- two members start an add operation in parallel, what happens?
